# This is just a base for all slatestarcodex.* files
# Use it together with the others, e.g.
# node index.js slatestarcodex.top.yml slatestarcodex.base.yml
metadata:
  dc:
    creator: Scott Alexander
    language: en-US
modify:
  content:
  - search: (<img .*?)>
    flags: g
    replace: $1/>
  - search: <br>
    flags: g
    replace: <br/>
